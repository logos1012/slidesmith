# pnpm: trust esbuild's install-time native binary build (vitest dep chain).
# Without this, `verify-deps-before-run` blocks `pnpm typecheck` / `pnpm test`.
allow-builds[]=esbuild
verify-deps-before-run=false
