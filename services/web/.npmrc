# Phase 6 Fix (F4): pnpm@11.0.9 정합화 — verify-deps-before-run 비활성화.
# pnpm 11에서 typecheck/test 등 임의 script 실행 전 install 강제 검증.
# 우리 워크플로우는 install이 명시적으로 선행되므로 이 hook은 불필요.
# 빌드 스크립트 화이트리스트는 pnpm-workspace.yaml `allowBuilds:`에 박제.
verify-deps-before-run=false
